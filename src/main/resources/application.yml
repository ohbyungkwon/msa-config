server:
  port: 8888

spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ohbyungkwon/msa-config-store.git
          #username: !!!!
          #passphrase: !!!
          #uri: file://Users/obyeong-gwon/git-local-repo
